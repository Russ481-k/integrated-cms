#!/bin/bash
# 🚨 이 파일은 동적 초기화 스크립트(00-dynamic-init-databases.sh)로 대체되었습니다.
# 레거시 호환성을 위해 동적 스크립트를 호출합니다.

echo "🔄 레거시 초기화 스크립트에서 동적 스크립트로 리다이렉트 중..."
exec bash /docker-entrypoint-initdb.d/00-dynamic-init-databases.sh